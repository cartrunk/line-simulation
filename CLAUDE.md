# CLAUDE.md — line-simulation

## Project Overview

This is the **line-simulation** repository. It is a new project with no existing source code yet.

## Repository State

- **Status**: Newly initialized, empty repository
- **Remote**: `cartrunk/line-simulation`
- **Primary branch**: To be established with the first commit

## Development Guidelines

### Git Workflow

- Develop on feature branches prefixed with `claude/`
- Write clear, descriptive commit messages
- Push with `git push -u origin <branch-name>`

### Code Standards (to be adopted as the project grows)

- Keep code simple and focused; avoid over-engineering
- Prefer editing existing files over creating new ones
- Do not introduce security vulnerabilities (XSS, injection, etc.)
- Add comments only where logic is non-obvious
- Write tests alongside new functionality

### File Organization

As the project is built out, document the directory structure and architecture decisions here.

## Build & Test

No build system or test framework has been configured yet. Update this section once tooling is added.

## Notes for AI Assistants

- Always read existing code before proposing changes
- Use TodoWrite to plan multi-step tasks
- Reference files with `file_path:line_number` format
- Keep changes minimal and directly relevant to the request
